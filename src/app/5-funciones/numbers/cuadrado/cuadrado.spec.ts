/* import { numeroCuadrado } from './cuadrado';

describe('Prueba de Número al cuadrado', () => {
    it('Función de al cuadrado', () => {
        const result = numeroCuadrado(2);
        expect(result).toBe(5);
    });
}); */